#!/bin/sh

PROGRAM=/usr/sbin/alsactl

# Exit silently if alsa-utils isn't installed
[ -x ${PROGRAM} ] || exit 0

echo -n " * saving mixer state ...     "
if ${PROGRAM} save > /dev/null 2> /dev/null; then
	echo "[   OK   ]"
else
	echo "[ FAILED ]"
	exit 1
fi
