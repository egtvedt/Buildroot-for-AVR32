#!/bin/sh

FBV=/usr/bin/fbv
SPLASHFILE="/etc/bootsplash.jpg"

echo -n "Bootsplash image: "
if [ ! -x "${FBV}" -o ! -f "${SPLASHFILE}" ]; then
	echo "missing"
	exit 1
fi

${FBV} ${SPLASHFILE} > /dev/null &
if [ $? -eq 0 ]; then
	echo "loaded"
else
	echo "failed"
fi
